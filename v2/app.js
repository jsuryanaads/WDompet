const KEY='wdompet_v2_transactions';
const FUNDS='wdompet_v2_funds';
const TRANSFERS='wdompet_v2_transfers';
const BUDGET='wdompet_v2_budget';
const $=id=>document.getElementById(id);
let tx=JSON.parse(localStorage.getItem(KEY)||'[]');
let funds=JSON.parse(localStorage.getItem(FUNDS)||'[]');
let transfers=JSON.parse(localStorage.getItem(TRANSFERS)||'[]');
const now=new Date(),today=new Date(now.getTime()-now.getTimezoneOffset()*60000).toISOString().slice(0,10);
function money(n){return new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(n)||0)}
function monthKey(d){return String(d||'').slice(0,7)}
function selected(){return $('monthFilter')?.value||monthKey(today)}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function shortMoney(n){if(n>=1000000)return 'Rp'+(n/1000000).toFixed(1).replace('.0','')+' jt';if(n>=1000)return 'Rp'+Math.round(n/1000)+' rb';return 'Rp'+Math.round(n)}
function initMonths(){const el=$('monthFilter');if(!el)return;let arr=[...new Set([monthKey(today),...tx.map(x=>monthKey(x.date))])].filter(Boolean).sort().reverse();el.innerHTML=arr.map(m=>`<option value="${m}">${new Date(m+'-01T00:00:00').toLocaleDateString('id-ID',{month:'long',year:'numeric'})}</option>`).join('');if(!arr.includes(el.value))el.value=monthKey(today)}
function chartMonths(){let base=new Date(selected()+'-01T00:00:00'),arr=[];for(let i=5;i>=0;i--){let d=new Date(base.getFullYear(),base.getMonth()-i,1);arr.push(d.toISOString().slice(0,7))}return arr}
function chartHtml(){let months=chartMonths(),values=months.map(m=>{let rows=tx.filter(x=>monthKey(x.date)===m);return{m,inc:rows.filter(x=>x.type==='income').reduce((a,x)=>a+x.amount,0),out:rows.filter(x=>x.type==='expense').reduce((a,x)=>a+x.amount,0)}});if(values.every(x=>x.inc===0&&x.out===0))return '<div class="chart-empty"><b>Belum ada data arus kas</b><span>Tambahkan transaksi untuk melihat grafik 6 bulan.</span></div>';let max=Math.max(1,...values.flatMap(x=>[x.inc,x.out])),W=760,H=270,L=48,R=18,T=18,B=42,iw=W-L-R,ih=H-T-B,x=i=>L+(i/(values.length-1))*iw,y=v=>T+ih-(v/max)*ih,fmt=m=>new Date(m+'-01T00:00:00').toLocaleDateString('id-ID',{month:'short'}).replace('.',''),path=k=>values.map((v,i)=>(i?'L':'M')+x(i).toFixed(1)+','+y(v[k]).toFixed(1)).join(' '),grid=[0,.25,.5,.75,1].map(p=>{let yy=T+ih-p*ih;return`<line x1="${L}" y1="${yy}" x2="${W-R}" y2="${yy}" class="gridline"/><text x="${L-8}" y="${yy+4}" text-anchor="end" class="axis">${shortMoney(max*p)}</text>`}).join(''),labels=values.map((v,i)=>`<text x="${x(i)}" y="${H-14}" text-anchor="middle" class="axis">${fmt(v.m)}</text>`).join(''),dots=values.map((v,i)=>`<circle cx="${x(i)}" cy="${y(v.inc)}" r="4" class="point pin"/><circle cx="${x(i)}" cy="${y(v.out)}" r="4" class="point pout"/>`).join('');return`<svg viewBox="0 0 ${W} ${H}" role="img" aria-label="Grafik pemasukan dan pengeluaran enam bulan"><g>${grid}</g><path d="${path('inc')}" class="line income-line"/><path d="${path('out')}" class="line expense-line"/>${dots}${labels}</svg><div class="chart-summary"><span>Terakhir: <b>${money(values[5].inc)}</b> masuk</span><span><b>${money(values[5].out)}</b> keluar</span></div>`}
function renderChart(){let html=chartHtml();if($('cashChart'))$('cashChart').innerHTML=html;if($('reportCashChart'))$('reportCashChart').innerHTML=html}
function renderCategories(target,data){if(!target)return;let rows=data.filter(x=>x.type==='expense').reduce((a,x)=>{a[x.category]=(a[x.category]||0)+x.amount;return a},{}),list=Object.entries(rows).sort((a,b)=>b[1]-a[1]),max=list[0]?.[1]||1;target.innerHTML=list.length?list.slice(0,8).map(([cat,val],i)=>`<div class="catrow" style="animation-delay:${i*.06}s"><div class="cathead"><span>${esc(cat)}</span><b>${money(val)}</b></div><div class="cattrack"><i style="width:${Math.round(val/max*100)}%"></i></div></div>`).join(''):'<div class="empty">Belum ada pengeluaran.</div>'}
function renderInsights(inc,out,data){if(!$('insights'))return;let balance=inc-out,budget=Number(localStorage.getItem(BUDGET+'_'+selected())||0),largest=data.filter(x=>x.type==='expense').sort((a,b)=>b.amount-a.amount)[0],items=[['Kondisi',balance>=0?`Surplus ${money(balance)}`:`Defisit ${money(Math.abs(balance))}`],['Transaksi',`${data.length} transaksi tercatat`]];if(largest)items.push(['Terbesar',`${esc(largest.category)} • ${money(largest.amount)}`]);if(budget)items.push(['Anggaran',`${Math.round(out/budget*100)}% terpakai`]);$('insights').innerHTML=items.map((x,i)=>`<div class="insight-item" style="animation-delay:${i*.08}s"><span>${x[0]}</span><p><b>${x[1]}</b></p></div>`).join('')}
function renderBudget(inc,out){if(!$('budgetInput'))return;let budget=Number(localStorage.getItem(BUDGET+'_'+selected())||0),pct=budget?Math.round(out/budget*100):0;$('budgetInput').value=budget||'';$('progressBar').style.width=Math.min(100,pct)+'%';$('budgetPercent').textContent=pct+'%';$('budgetText').textContent=budget?`${money(out)} dari ${money(budget)}`:'Belum ada anggaran';let remaining=budget-out;$('budgetInsight').innerHTML=budget?[['Status',remaining>=0?`Sisa ${money(remaining)}`:`Melebihi ${money(Math.abs(remaining))}`],['Rasio',`${pct}% dari anggaran telah digunakan`]]:[['Status','Belum ada anggaran untuk periode ini'],['Saran','Tetapkan batas pengeluaran agar kontrol keuangan lebih mudah.']].map((x,i)=>`<div class="insight-item"><span>${x[0]}</span><p><b>${x[1]}</b></p></div>`).join('')}
function renderReports(inc,out){if(!$('reportBalance'))return;$('reportBalance').textContent=money(inc-out);$('reportIncome').textContent=money(inc);$('reportExpense').textContent=money(out);renderCategories($('reportCategoryChart'),tx.filter(x=>monthKey(x.date)===selected()))}
function render(){if(!$('balance'))return;let m=selected(),data=tx.filter(x=>monthKey(x.date)===m),inc=data.filter(x=>x.type==='income').reduce((a,x)=>a+x.amount,0),out=data.filter(x=>x.type==='expense').reduce((a,x)=>a+x.amount,0),balance=inc-out;$('balance').textContent=money(balance);$('income').textContent=money(inc);$('expense').textContent=money(out);if($('incomeMini'))$('incomeMini').textContent=money(inc);if($('expenseMini'))$('expenseMini').textContent=money(out);if($('ratioMini'))$('ratioMini').textContent=inc?Math.round(out/inc*100)+'%':'0%';if($('balanceStatus'))$('balanceStatus').textContent=balance>=0?'● Keuangan positif':'● Pengeluaran melebihi pemasukan';if($('transactionMeta'))$('transactionMeta').textContent=data.length+' transaksi';if($('list')){let q=($('search')?.value||'').toLowerCase(),f=$('typeFilter')?.value||'all';data=data.filter(x=>(f==='all'||x.type===f)&&[x.date,x.category,x.note,x.type].join(' ').toLowerCase().includes(q)).sort((a,b)=>b.date.localeCompare(a.date)||b.created-a.created);$('list').innerHTML=data.map(x=>`<tr><td>${new Date(x.date+'T00:00:00').toLocaleDateString('id-ID')}</td><td><span class="badge ${x.type}">${x.type==='income'?'Pemasukan':'Pengeluaran'}</span></td><td>${esc(x.category)}</td><td>${esc(x.note||'-')}</td><td class="${x.type==='income'?'plus':'minus'}">${x.type==='income'?'+':'-'} ${money(x.amount)}</td><td><button class="del" data-id="${esc(x.id)}">Hapus</button></td></tr>`).join('');if($('empty'))$('empty').style.display=data.length?'none':'block'}renderChart();renderCategories($('categoryChart'),tx.filter(x=>monthKey(x.date)===m));renderInsights(inc,out,tx.filter(x=>monthKey(x.date)===m));renderBudget(inc,out);renderReports(inc,out)}
function removeTx(id){if(confirm('Hapus transaksi ini?')){tx=tx.filter(x=>x.id!==id);localStorage.setItem(KEY,JSON.stringify(tx));initMonths();render()}}
function exportCsv(){let rows=[['Tanggal','Jenis','Kategori','Keterangan','Nominal'],...tx.map(x=>[x.date,x.type==='income'?'Pemasukan':'Pengeluaran',x.category,x.note,x.amount])],csv=rows.map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(',')).join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download='wdompet-transaksi.csv';a.click();URL.revokeObjectURL(a.href)}
function renderFunds(){let target=$('fundList');if(!target)return;let balances={};funds.forEach(f=>balances[f.id]=Number(f.openingBalance)||0);tx.forEach(x=>{if(x.fundId&&balances[x.fundId]!=null)balances[x.fundId]+=x.type==='income'?Number(x.amount): -Number(x.amount)});transfers.forEach(t=>{if(balances[t.fromFundId]!=null)balances[t.fromFundId]-=Number(t.amount);if(balances[t.toFundId]!=null)balances[t.toFundId]+=Number(t.amount)});$('fundMeta').textContent=funds.length+' sumber dana';target.innerHTML=funds.length?`<table><thead><tr><th>Nama</th><th>Jenis</th><th>Keterangan</th><th>Saldo</th><th>Aksi</th></tr></thead><tbody>${funds.map(f=>`<tr><td><b>${esc(f.name)}</b></td><td><span class="fund-badge">${({bank:'Bank',ewallet:'E-Wallet',cash:'Tunai',other:'Lainnya'})[f.type]||'Lainnya'}</span></td><td>${esc(f.masked||'-')}</td><td class="fund-balance">${money(balances[f.id])}</td><td><button class="fund-del" data-fund-id="${esc(f.id)}">Hapus</button></td></tr>`).join('')}</tbody></table>`:'<div class="fund-empty">Belum ada sumber dana.</div>'}
function saveFund(){let name=$('fundName')?.value.trim();if(!name)return alert('Nama sumber dana wajib diisi.');let f={id:crypto.randomUUID?crypto.randomUUID():String(Date.now()),name,type:$('fundType').value,openingBalance:Number($('fundOpening').value)||0,masked:$('fundMasked').value.trim(),active:true,created:Date.now()};funds.push(f);localStorage.setItem(FUNDS,JSON.stringify(funds));$('fundName').value='';$('fundOpening').value='';$('fundMasked').value='';renderFunds()}
function removeFund(id){if(!confirm('Hapus sumber dana ini?'))return;if(tx.some(x=>x.fundId===id)||transfers.some(x=>x.fromFundId===id||x.toFundId===id))return alert('Sumber dana masih digunakan transaksi/transfer.');funds=funds.filter(f=>f.id!==id);localStorage.setItem(FUNDS,JSON.stringify(funds));renderFunds()}
function themeInit(){let dark=localStorage.getItem('wdompet_theme')==='dark';document.body.classList.toggle('theme-dark',dark);if($('themeToggle'))$('themeToggle').textContent=dark?'☀':'☼'}
function bind(){if($('date'))$('date').value=today;if($('monthFilter'))$('monthFilter').addEventListener('change',render);if($('search'))$('search').addEventListener('input',render);if($('typeFilter'))$('typeFilter').addEventListener('change',render);if($('list'))$('list').addEventListener('click',e=>{let b=e.target.closest('.del');if(b)removeTx(b.dataset.id)});if($('form'))$('form').addEventListener('submit',e=>{e.preventDefault();let amount=Number($('amount').value);if(!Number.isFinite(amount)||amount<=0)return alert('Nominal tidak valid.');tx.push({id:crypto.randomUUID?crypto.randomUUID():String(Date.now()),type:$('type').value,date:$('date').value,category:$('category').value,note:$('note').value.trim(),amount,created:Date.now()});localStorage.setItem(KEY,JSON.stringify(tx));e.target.reset();if($('date'))$('date').value=today;initMonths();if($('monthFilter'))$('monthFilter').value=monthKey(today);render();location.href='index.html'});if($('saveBudget'))$('saveBudget').addEventListener('click',()=>{let n=Number($('budgetInput').value)||0;localStorage.setItem(BUDGET+'_'+selected(),String(n));render()});if($('saveFund'))$('saveFund').addEventListener('click',saveFund);if($('fundList'))$('fundList').addEventListener('click',e=>{let b=e.target.closest('.fund-del');if(b)removeFund(b.dataset.fundId)});if($('themeToggle'))$('themeToggle').addEventListener('click',()=>{let dark=!document.body.classList.contains('theme-dark');document.body.classList.toggle('theme-dark',dark);localStorage.setItem('wdompet_theme',dark?'dark':'light');$('themeToggle').textContent=dark?'☀':'☼'});if($('exportBtn'))$('exportBtn').addEventListener('click',exportCsv);if($('reportExport'))$('reportExport').addEventListener('click',exportCsv)}
window.addEventListener('storage',e=>{if(e.key===KEY)tx=JSON.parse(e.newValue||'[]');if(e.key===FUNDS)funds=JSON.parse(e.newValue||'[]');if(e.key===TRANSFERS)transfers=JSON.parse(e.newValue||'[]');if(e.key===BUDGET+'_'+selected()||e.key===KEY||e.key===FUNDS||e.key===TRANSFERS){initMonths();render();renderFunds()}if(e.key==='wdompet_theme'){document.body.classList.toggle('theme-dark',e.newValue==='dark');if($('themeToggle'))$('themeToggle').textContent=e.newValue==='dark'?'☀':'☼'}});
bind();themeInit();initMonths();render();renderFunds();